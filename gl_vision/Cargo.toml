# ABOUTME: Computer vision and motion detection algorithms
# ABOUTME: Analyzes video frames for motion and visual changes

[package]
name = "gl_vision"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
gl_core = { path = "../gl_core" }
